<%= form_for(@comment) do |f| %>
  <%= f.hidden_field :post_id %>
  <%= f.text_area :content %>
  </br>
  You: <%= f.collection_select :author_id, Author.all, :id, :name %>
  </br>
  <%= f.submit %>
  </br>
<% end %>
